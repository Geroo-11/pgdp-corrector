# Clonar repositorio

git clone <URL>

# Crear entorno virtual dentro del proyecto

cd TPFinal
python3 -m venv .venv

# Activarlo

source .venv/bin/activate

# Instalar paquetes

pip install -r requirements.txt